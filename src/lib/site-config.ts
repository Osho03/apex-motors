/**
 * APEX MOTORS // SITE CONFIGURATION
 * Replace CONTACT_EMAIL with the inbox that should receive VIP consultation
 * requests. Requests are delivered via FormSubmit (no backend required).
 */
export const CONTACT_EMAIL = "zenpixel03@gmail.com";

export const CONTACT_FORM_ENDPOINT = `https://formsubmit.co/ajax/${CONTACT_EMAIL}`;